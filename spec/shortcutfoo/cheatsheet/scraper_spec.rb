RSpec.describe Shortcutfoo::Cheatsheet::Scraper do
  it "has a version number" do
    expect(Shortcutfoo::Cheatsheet::Scraper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
